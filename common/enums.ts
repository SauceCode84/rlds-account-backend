
export enum RouteParamTypes {
  REQUEST,
  RESPONSE,
  NEXT,
  SESSION,
  HEADERS,
  QUERY,
  BODY,
  PARAM
}

export enum RequestMethod {
    GET = 0,
    POST,
    PUT,
    DELETE,
    PATCH,
    ALL,
}
